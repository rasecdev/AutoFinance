services:
  producao:
    build: .
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    restart: unless-stopped

  homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    restart: unless-stopped

  backup-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/backup.js; sleep 86400; done"
    restart: unless-stopped

  backup-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/backup.js; sleep 86400; done"
    restart: unless-stopped

  monitor-precos-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/monitorarPrecos.js; sleep 604800; done"
    restart: unless-stopped

  monitor-precos-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/monitorarPrecos.js; sleep 604800; done"
    restart: unless-stopped

  relatorio-semanal-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/relatorioSemanal.js; done"
    restart: unless-stopped

  relatorio-semanal-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/relatorioSemanal.js; done"
    restart: unless-stopped

  relatorio-mensal-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/relatorioMensal.js; done"
    restart: unless-stopped

  relatorio-mensal-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/relatorioMensal.js; done"
    restart: unless-stopped

  verificar-despesas-fixas-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/verificarDespesasFixas.js; done"
    restart: unless-stopped

  verificar-despesas-fixas-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/verificarDespesasFixas.js; done"
    restart: unless-stopped

  ler-email-faturas-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/lerEmailFaturas.js; done"
    restart: unless-stopped

  ler-email-faturas-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/lerEmailFaturas.js; done"
    restart: unless-stopped

  sincronizar-calendario-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/sincronizarCalendario.js; done"
    restart: unless-stopped

  sincronizar-calendario-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/sincronizarCalendario.js; done"
    restart: unless-stopped

  sincronizar-open-finance-producao:
    image: autofinance:latest
    env_file: .env.producao
    volumes:
      - producao-db:/app/data
    command: sh -c "while true; do node dist/scripts/sincronizarOpenFinance.js; done"
    restart: unless-stopped

  sincronizar-open-finance-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/sincronizarOpenFinance.js; done"
    restart: unless-stopped

  renovar-sandbox-pluggy-homologacao:
    image: autofinance:latest
    env_file: .env.homologacao
    volumes:
      - homologacao-db:/app/data
    command: sh -c "while true; do node dist/scripts/renovarSandboxPluggy.js; done"
    restart: unless-stopped

volumes:
  producao-db:
  homologacao-db:
