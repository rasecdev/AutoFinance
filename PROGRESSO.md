# Progresso — AutoFinance

Log vivo do projeto. Atualizado a cada marco/etapa concluída — é o primeiro arquivo a ser lido antes de continuar o trabalho, pra saber o que já foi feito e qual o próximo passo. Design e roadmap completo ficam em [PLANO.md](PLANO.md) (inclui "Decisões em aberto" e "Fora de escopo" — **confira ali antes de reabrir qualquer pergunta de arquitetura do zero**, ver achado de 2026-09-11 abaixo); aqui fica o estado atual e o índice do histórico completo.

## Estado atual

- **Fase 6 (parte 13) — Leitura de planilha (Excel) + correção de PDF: concluída (2026-09-14).** Pedido do usuário logo após o teste manual da parte 12 (queria imagem + PDF + Excel funcionando de verdade). Duas peças: **(1) correção de PDF** — causa raiz real encontrada por pesquisa (documentação oficial do OpenRouter): PDF precisa do content type `file` dedicado (`{type:'file', file:{filename, file_data}}`), não `image_url` (usado só pra imagem) — `extrairComprovante` corrigido para montar o bloco certo por `mimeType`. **(2) leitura de planilha `.xlsx`** — parser estruturado (`read-excel-file`, só leitura, aceita `Buffer`), não IA de visão (planilha é dado tabular, não imagem); `interpretarPlanilha` (chamada de texto dedicada, fora do loop de tool calling, mesmo padrão de `gerarAnaliseQualidade`) mapeia colunas arbitrárias em transações estruturadas. Conta/cartão vem da legenda (caption) da mensagem do Telegram — sem "turno de modelo" pra perguntar, já que este fluxo não passa por `conversa_texto`. Nova tool `registrar_transacoes_em_lote` (sempre `requerConfirmacao: true`, diferente de `registrar_transacao`) registra tudo de uma vez; a pendência é montada DIRETO pelo handler (`definirPendencia`), sem o modelo re-serializar uma lista grande — reaproveita 100% do mecanismo de confirmação síncrona já existente desde a Fase 3, só com uma mensagem de confirmação própria (resumo, não o JSON cru) pra não estourar o limite do Telegram numa planilha grande. **Achado real ao instalar a dependência**: a lib inicialmente cogitada (`xlsx`/SheetJS) tem vulnerabilidade de alta severidade sem correção disponível (prototype pollution + ReDoS, quebraria `npm audit` no CI); `exceljs` trouxe vulnerabilidade moderada transitiva. Trocado por `read-excel-file` (0 vulnerabilidades) — **só `.xlsx` nesta rodada, sem `.xls`/`.csv`** (limitação documentada). 5 tarefas (84-88) via milestone "Fase 6 (parte 13)" (issues #227-231, PRs #232-236), todas fechadas automaticamente via `Closes #N`. 721 testes passando, build/lint limpos. **Teste manual em Homologação via Telegram**: PDF real de comprovante lido corretamente (correção confirmada) e planilha `.xlsx` de teste (gerada com dados fictícios de extrato — mercado, salário, contas, etc.) com legenda de conta, até a confirmação e registro em lote — confirmado pelo usuário ("feito e testado, deu certo"). Checkpoint fechado, milestone fechado.
- **Fase 6 (parte 12) — Leitura de comprovante (foto/PDF): concluída (2026-09-13). Fecha a fase inter-fases de entrada por voz/imagem.** `handlerMidia` (antes só stub) agora extrai dado estruturado de foto de comprovante via IA de visão (`google/gemini-2.5-flash-lite`, roteado como qualquer outro fluxo — `extrairComprovante` em `src/ai/extracaoComprovante.ts`, chamada multimodal dedicada fora do loop de tool calling, mesmo padrão de `gerarAnaliseQualidade`). Três desfechos possíveis: não é comprovante (mensagem direta), é fatura de cartão/boleto de dívida (degrada com aviso — correspondência com fatura/parcela fica pra Fase 7), ou é comprovante de compra (monta mensagem sintética e chama o MESMO pipeline de `conversa_texto`, via `processarMensagemTexto`, como se o usuário tivesse digitado). **Confirmação obrigatória antes de gravar, sem mecanismo novo**: `exigirConfirmacaoDeRegistro(tools)` força `requerConfirmacao: true` só em `registrar_transacao` pra essa chamada específica, reaproveitando 100% do mecanismo de confirmação síncrona já existente desde a Fase 3 — cumpre o item 6 do OWASP (conteúdo externo não confiável nunca grava sem confirmação explícita) sem tabela nova, sem tool nova, sem estado novo. 5 tarefas (79-83) via milestone "Fase 6 (parte 12)" (issues #217-221, PRs #222-226), todas fechadas automaticamente via `Closes #N`. 701 testes passando, build/lint limpos. **Teste manual em Homologação via Telegram**: foto de comprovante real reconhecida corretamente, distinguindo o que é comprovante do que não é — confirmado pelo usuário. **Achado real confirmado (PDF)**: Gemini 2.5 Flash Lite via OpenRouter rejeita PDF na chamada multimodal (data URI `application/pdf`) — o handler degradou como projetado, respondendo a mensagem específica ("ainda não consigo ler PDF, manda como foto") sem travar nem afetar o caminho de foto. Fica documentado como limitação conhecida (ver "Erros conhecidos"); suporte a PDF de verdade exigiria endpoint dedicado de parsing de documento (fora de escopo por ora). Checkpoint fechado, milestone fechado.
- **Fase 6 (parte 11) — Transcrição de voz: concluída (2026-09-13).** Primeira parte da nova fase entre Fase 6 e Fase 7 (a pedido do usuário, entrada por voz e por foto/PDF de comprovante — Fase 7 já pressupõe a leitura de comprovante existir, ver linha 580 do PLANO.md). Mensagem de voz no Telegram (`message:voice`) é baixada, transcrita via `client.audio.transcriptions.create` contra o endpoint nativo de transcrição do OpenRouter (`openai/whisper-large-v3-turbo`, roteado como qualquer outro fluxo), e o texto resultante alimenta o mesmo pipeline de `conversa_texto` já existente por completo — zero tool nova, zero fluxo de confirmação novo (`processarMensagemTexto`, extraído de `texto.ts` pra ser reaproveitado pelos dois handlers). 4 tarefas (75-78) via milestone "Fase 6 (parte 11)" (issues #209-212, PRs #213-216). 681 testes (1 flake isolado de timeout já documentado). **Achado real de pesquisa, corrigido no PLANO.md**: a suposição de que STT ficava "fora do OpenRouter" (exigindo Groq/Deepgram dedicado) estava desatualizada — o OpenRouter ganhou endpoint próprio de transcrição, mesma chave já usada em tudo, e mais barato que a Groq direta (~$0,0108/hora vs ~$0,04/hora pro Whisper Turbo). **Teste manual em Homologação via Telegram**: áudio real com pedido simples, ação certa executada — confirmado pelo usuário. Checkpoint fechado, milestone fechado.
- **Fase 6 (parte 10) — Consulta dinâmica + gráfico: concluída (2026-09-13). Fecha a Fase 6 inteira.** Três ferramentas novas: `consultar_dados_dinamico` (métrica/dimensão/domínio de uma whitelist fixa — financeiro: `soma_valor`/`media_valor`/`contagem`/`saldo` sobre `transacoes`; `uso_ia`: mesmas métricas sobre `custo_estimado` de `uso_tokens`, excluindo `origem='benchmark_interno'` — nunca SQL livre gerado pela IA), `gerar_grafico` (barra/linha/pizza via `chartjs-node-canvas`, renderiza dado já calculado, nunca inventado) e `consultar_e_graficar` (atalho combinando as duas numa só chamada). Exigiu mudança de arquitetura: `ToolDefinition.handler` agora pode devolver `{texto, imagem}` além de `string` — a imagem nunca vai pro contexto do modelo, só é acumulada e mandada como foto no Telegram depois do texto. 7 tarefas (68-74) via milestone "Fase 6 (parte 10)" (issues #191-197, PRs #198-204). 668 testes. **Três achados reais de teste manual em Homologação, todos corrigidos e reconfirmados**: (1, issue #205) sem período informado, as duas tools de consulta agregavam TODO o histórico (nunca "mês atual" como o resto do sistema promete — regra 2 do SYSTEM_PROMPT) — corrigido com default de mês atual (exceto quando `agrupar_por` inclui `mes`, caso legítimo de tendência multi-mês) + eco de interpretação sempre mostrando o período efetivo usado; bug lateral junto, filtro de `data_hora` (uso_ia) por comparação léxica de string excluía o último dia do período; (2, issue #207) pedindo gráfico, o modelo chamava `consultar_e_graficar` E `gerar_grafico` juntas na mesma resposta (foto duplicada) e ainda tentava desenhar a imagem ele mesmo em base64 alucinado no texto — corrigido com descrição explícita nas duas tools (nunca chamar juntas) + regra 13 nova no SYSTEM_PROMPT (nunca embutir/codificar imagem no texto). Checkpoint fechado, milestone fechado (9 issues no total: 7 tarefas + 2 bugs).
- **Fase 6 (parte 9) — Projeção financeira: concluída (2026-09-12).** Quatro peças de leitura, nenhuma tabela nova: `projetar_fluxo_caixa(dias)` (soma parcelas pendentes + faturas abertas + despesas fixas ativas sem cartão vencendo numa janela contra o saldo atual, marcando a data em que ficaria negativo), `consultar_patrimonio_liquido()` (saldo de contas menos dívida, PF/PJ/consolidado), `simular_amortizacao` (mesma fórmula Price/SAC de `amortizar_divida`, hipotético, nunca grava) e alerta de limite de cartão (80%, embutido na resposta de `registrar_transacao`). 7 tarefas (61-67) via milestone "Fase 6 (parte 9)" (issues #175-181, PRs #182-188). 627 testes (1 flake isolado de timeout já documentado). **Dois achados reais registrados em `tasks/plan.md` e corrigidos**: (1) o PLANO.md original citava `divida_id` como parâmetro de `simular_amortizacao` — corrigido pra conta+tipo_divida, mesma convenção do resto do projeto (nenhuma tool usa id de dívida); (2) nenhum código do projeto mantém `faturas.valor` a partir de transação registrada (faturas só existem via seed/renegociação) — o alerta de limite de cartão por isso calcula o gasto direto de `transacoes` (ciclo atual via `dia_fechamento`), não depende desse campo. **Teste manual em Homologação (2026-09-12/13) via Telegram**: `projetar_fluxo_caixa`, `consultar_patrimonio_liquido` e o alerta de limite de cartão passaram de primeira. `simular_amortizacao` revelou 2 bugs reais de UX, ambos corrigidos e reconfirmados: (1) o modelo (`gpt-4o-mini`) às vezes parafraseava o resultado da simulação no tempo verbal de algo já realizado ("ao quitar parte da dívida antecipadamente"), sem repetir o aviso "nada foi alterado" da tool — corrigido com a regra 12 do `SYSTEM_PROMPT` (sempre narrar simulação/hipótese no condicional); (2) a tool exigia conta obrigatória (copiado de `amortizar_divida`, que precisa ser estrito porque grava dado real) mesmo sendo simulação pura — o modelo ficava presa pedindo a conta mesmo com a dívida sendo única no sistema, corrigido com nova `resolverDividaGlobal` (busca sem conta, só pede pra especificar quando há ambiguidade real). Checkpoint fechado.
- **Fase 6 (parte 8) — analisar_qualidade(periodo): concluída (2026-09-12).** Nova tool de chat que agrega `interacoes_ia` (taxa de correção manual por fluxo/modelo) e `erros_execucao` (contagem por contexto) do período atual e do anterior, manda só os números agregados pra IA (nunca conteúdo de conversa) e recebe uma análise narrativa com sugestão de ação — mesmo mecanismo de `relatorioMensal.ts`, mas via tool sob demanda, não job agendado. Nova tabela `analises_qualidade` guarda o histórico (só log auditável, não relido pela tool nesta rodada). 6 tarefas (55-60) via milestone "Fase 6 (parte 8)" (issues #162-167, todas fechadas via `Closes #N`, PRs #168-173). 586 testes passando, build/lint limpos. **Teste manual real em Homologação via Telegram**: pergunta "como estão as respostas da IA esse mês?" disparou a tool corretamente e a análise (gerada pelo `deepseek/deepseek-v4-flash`, já roteado nesse fluxo) identificou de forma independente, só a partir do dado agregado, o problema real e já conhecido do `openai/gpt-5-nano` (ver "Erros conhecidos" abaixo) — confirma que a tool funciona como projetado. Checkpoint fechado.
- **Fase 6 (parte 7) — Job mensal de despesas fixas: concluída (2026-09-12).** Novo `verificarDespesasFixas.ts` roda no último dia do mês às 23h (mesmo agendamento de `relatorioMensal.ts`) e avisa via Telegram só quando uma despesa fixa ativa (sem cartão) não tem transação correspondente (mesma conta+categoria, case-insensitive) no período — mesmo princípio de "só alerta quando há pendência" já usado em `monitorarPrecos.ts`. Quebrada em 5 tarefas (50-54) via `planning-and-task-breakdown`, `tasks/plan.md`/`tasks/todo.md` (conteúdo da Fase 6 parte 6 preservado no histórico do git). **Primeira rodada com o fluxo de milestone/issues do GitHub** (registrado no CLAUDE.md em 2026-09-11) — milestone "Fase 6 (parte 7)" com 5 issues (#156-160), todas fechadas automaticamente via `Closes #N` no merge do PR #161. 572 testes (1 flake isolado de timeout em `tests/db/migrate.test.ts`, já documentado, confirmado não-relacionado rodando isolado). Checkpoint "Job mensal de despesas fixas funcional" fechado após teste manual real em Homologação (ver achado abaixo).
- **Teste manual em Homologação (2026-09-12):** sem tool de chat pra rodar o cenário via Telegram nesta sessão (exigiria digitar como o próprio usuário), a verificação foi feita direto no banco de Homologação via SSH — despesa fixa de teste criada (`Teste E2E Fase6P7`, conta "Corrente", sem cartão), job rodado com `--agora`: alerta disparado e enviado de verdade pro chat de Homologação (confirmável no Telegram). Registrada uma transação da mesma conta+categoria no período e rodado de novo: log confirmou "nenhuma despesa fixa faltante no período", sem enviar mensagem — os dois lados do comportamento (alerta com pendência, silêncio sem pendência) confirmados com dado real, não só teste automatizado. Dado de teste limpo depois (transação excluída logicamente, despesa fixa pausada).
- **Reorganização do PROGRESSO.md (2026-09-11), a pedido do usuário** ("virou praticamente um livro"): o histórico detalhado (antes 367 linhas / 263KB num arquivo só, obrigatório de ler no início de toda sessão) foi movido pra `docs/progresso/`, dividido por período — ver índice abaixo. Este arquivo agora só guarda o estado atual e um ponteiro pro histórico, sem precisar carregar tudo.
- **Achado real (2026-09-11): conflito entre duas decisões de arquitetura sobre categoria, resolvido.** Auditoria feita ao reorganizar este arquivo encontrou um ADR de 2026-08-31 (`docs/adr/0001-categoria-tabela-auto-referenciada.md`, decidido via skill Wayfinder) nunca implementado, dizendo que `categorias` deveria ser tabela própria com hierarquia — contradizendo a decisão da Fase 6 parte 6 (texto livre), tomada sem saber que esse ADR existia. Resolvido com [ADR 0002](docs/adr/0002-categoria-continua-texto-livre.md): mantido texto livre (já implementado e testado), ADR 0001 marcado como superseded, pesquisa de taxonomia (Organizze/Mobills) preservada como referência futura em "Fora de escopo" do PLANO.md. `CONTEXT.md` e o "Modelo de dados" do PLANO.md corrigidos pra não citar mais `categoria_id`/`categorias`.
- **Roteamento `resumir_contexto`/`relatorio_mensal` trocado pra `deepseek/deepseek-v4-flash` (2026-09-12), validado em Homologação.** Motivação: os dois fluxos usavam o mesmo `gpt-4o-mini` do `conversa_texto` sem necessidade — são tarefas mais simples (resumo/narrativa curta) que não exigem tool calling. Sem caso de teste de benchmark interno pra esses fluxos (`casos_teste_benchmark` só cobre `conversa_texto` hoje), então a comparação foi feita via pesquisa de preço/benchmark público do OpenRouter + teste manual real (`node dist/scripts/relatorioMensal.js --agora` em Homologação) — qualidade do resumo em PT aprovada pelo usuário. Criado `src/scripts/definirRoteamento.ts` (`node dist/scripts/definirRoteamento.js <fluxo> <modelo>`) como ferramenta operacional pra gravar `roteamento_tarefas`, que antes só era possível via teste automatizado ou SQL bruto. Plano B combinado se o deepseek se mostrar instável com uso real: `openai/gpt-4.1-nano`.
- **Fase 7 — em andamento (iniciada 2026-09-15).** Planejada via `tasks/plan.md`/`tasks/todo.md` (milestone "Fase 7 (e-mail e calendário)", issues #237-244). **Tarefa 89 concluída (2026-09-15):** migration `0011_google_integracao.sql` — tabela `emails_processados` (idempotência do job de leitura de e-mail, `gmail_message_id UNIQUE`) e coluna `evento_calendario_id` (nullable) em `faturas`/`parcelas`, pra rastrear o evento do Google Calendar sem duplicar. PR #245 mergeado em `development` (`Closes #237`). **Tarefa 90 concluída (2026-09-15):** `env.ts` ganha grupo opcional `GOOGLE_CLIENT_ID`/`CLIENT_SECRET`/`REFRESH_TOKEN`/`CALENDAR_ID` — ausente por completo = `env.google === null` (integração desligada, caminho válido em Homologação), incompleto = erro explícito, completo = `env.google` populado (`calendarId` default `'primary'`). PR #246 mergeado (`Closes #238`). **Tarefa 91 concluída (2026-09-15):** `src/integracoes/google/auth.ts` (`criarClientesGoogle` monta clients Gmail/Calendar autenticados por um `OAuth2Client` compartilhado) + script manual `configurarGoogleOAuth.ts` (roda uma vez por ambiente, nunca chamado por job — imprime URL de consentimento, lê código via stdin, devolve refresh_token). `googleapis` instalado, `npm audit` limpo (não precisou do Plano B `google-auth-library`). PR #247 mergeado (`Closes #239`). **Tarefa 92 concluída (2026-09-15):** `src/db/repositories/correspondenciaFaturaParcela.ts` — `encontrarFaturaCorrespondente` (busca exata cartao_id+mes_referencia) e `encontrarParcelaCorrespondente` (exata por número quando informado; senão aproximação por valor ±1% + janela de data ±5 dias entre parcelas pendentes da dívida, com ambiguidade explícita quando mais de uma candidata bate — nunca resolve sozinho). PR #248 mergeado (`Closes #240`). **Tarefa 93 concluída (2026-09-15):** tools `registrar_fatura_email`/`registrar_parcela_email` (`requerConfirmacao: true` sempre), upsert via as funções de correspondência da Tarefa 92, `registrar_parcela_email` sempre grava `origem='email'`+`trace_id` e nunca resolve ambiguidade sozinho. Novas `criarFatura`/`atualizarValorFatura`/`criarParcelaEmail`/`atualizarParcelaEmail` nos repositórios. Registradas em `montarToolsConversa`. PR #249 mergeado (`Closes #241`) — 1 flake isolado de timeout em `consultaEGraficar.test.ts` no CI, já documentado, resolvido com re-run. **Tarefa 94 concluída (2026-09-15):** job `src/scripts/lerEmailFaturas.ts` — polling via Gmail (`has:attachment newer_than:30d`, intervalo de 3h), idempotente via `emails_processados`, reaproveita `extrairComprovante` (Fase 6) e as tools da Tarefa 93, monta pendência de confirmação por chat permitido. **Achado real:** `extrairComprovante` não capturava nenhum dado pra identificar QUAL cartão/dívida um documento se refere — adicionado campo opcional `identificador` ao schema de extração (não quebra o fluxo de foto da Fase 6) + `resolverDividaPorIdentificador` novo (busca por texto livre, sem exigir tipo) + repositório `emailsProcessados.ts`. PR #250 mergeado (`Closes #242`), CI verde de primeira. **Tarefa 95 concluída (2026-09-15):** job `src/scripts/sincronizarCalendario.ts` — varre faturas abertas/parcelas pendentes com vencimento dentro de 60 dias, cria/atualiza/remove evento no Google Calendar por `(tipo, referencia_id)` via `evento_calendario_id`, nunca duplica; update só é chamado quando `events.get` mostra que summary/data realmente mudaram. PR #251 mergeado (`Closes #243`), CI verde de primeira. **Tarefa 96 concluída (2026-09-15) — fecha o código da Fase 7 por completo (Tarefas 89-96, milestone "Fase 7 (e-mail e calendário)", issues #237-244, todas fechadas via `Closes #N`):** 4 serviços novos no `docker-compose.yml` (`ler-email-faturas`/`sincronizar-calendario` × produção/homologação). **Achado real:** as duas jobs saíam imediatamente sem dormir quando `env.google === null` — combinado com o loop `while true; do node ...; done` do compose, viraria busy-loop reiniciando sem parar assim que a integração estivesse desligada; corrigido com `dormirAte` no mesmo intervalo do polling normal antes de sair nesse caminho. PR #252 mergeado (`Closes #244`), CI verde (build/lint/770+ testes/`npm audit` limpos).
- **Checkpoint da Fase 7 — código completo, teste manual pendente.** `npm run build`/`lint`/`test` sem erro (checkpoint parcialmente fechado). **Falta:** teste manual real em Homologação (conta de teste real ou integração mantida desligada — decisão do usuário; e-mail de teste com fatura/boleto anexado; confirmação via Telegram; evento aparecendo no Google Calendar sem duplicar) — não executável por mim (exige consentimento OAuth via navegador com conta real).
- **Comando `/registrar_email` (2026-09-15), a pedido do usuário — alternativa ao script `configurarGoogleOAuth.ts` via terminal.** Usuário digita `/registrar_email` no bot, recebe o link de consentimento (uma autorização cobre Gmail + Calendar de uma vez), autoriza, cola o código de volta como mensagem normal no chat — bot troca por token e devolve o `refresh_token`. Passo final ainda manual (colar no `.env.*` e reiniciar os jobs — projeto não tem servidor HTTP público pra um callback OAuth de verdade, decisão explícita de não criar essa infraestrutura só pra isso). Ambiente já vinculado exige `/registrar_email confirmar` pra gerar vínculo novo. `env.ts` ganhou `googleOAuthClient` (par cliente sozinho, sem refresh token ainda, deixou de ser erro de configuração — estado intermediário válido). PR #253 mergeado em `development`, 775 testes passando (2 flakes isolados de timeout já documentados).
- **Achado crítico real de teste manual (2026-09-17): confirmação de fatura/parcela por e-mail nunca executava — corrigido.** Usuário testou de ponta a ponta pela primeira vez (conta de teste Google criada, `/registrar_email` usado com sucesso, PDF de fatura de teste enviado por e-mail, job `lerEmailFaturas --agora` rodado manualmente). A mensagem de confirmação chegou certinho no Telegram, mas responder "sim" não registrava a fatura — a IA tentava fazer outra coisa (recriar um cartão) por conta própria, num loop confuso. **Causa raiz:** `lerEmailFaturas.ts` roda em processo/container separado do bot; a pendência de confirmação (`definirPendencia`, Map em memória) era gravada na memória DAQUELE processo, que termina logo em seguida — o bot principal (processo diferente, memória própria) nunca via essa pendência, então tratava o "sim" como mensagem normal de chat. **Corrigido** com tabela nova `confirmacoes_pendentes` (migration 0012) como canal compartilhado de verdade entre processos (só nome da tool + argumentos serializados, nunca a função em si); `processarMensagemTexto` passou a checar essa tabela como fallback. Fluxos intra-processo (conversa normal, planilha) continuam usando o Map em memória, sem mudança. 21 testes novos, build/lint/suite completa passando. Confirmado pelo usuário em Homologação, com o fix já implantado ("sim confirmei, e fez").
- **Achado real de teste manual (2026-09-17): `consultar_fatura` sem mês informado consultava o mês errado — corrigido.** Usuário pediu a fatura de um cartão sem dizer o mês e recebeu resposta pro mês seguinte, não o atual — violando a regra 2 do SYSTEM_PROMPT ("sem período informado, o sistema assume o atual sozinho, nunca o modelo inventa"). **Causa raiz:** `mes_referencia` era campo obrigatório no schema Zod de `consultar_fatura`, forçando o modelo a inventar um valor sempre que o usuário não informava nenhum mês. **Corrigido** (PR #258): campo virou opcional, `normalizarMesReferencia(undefined)` agora devolve o mês atual real (calculado no código, nunca pelo modelo) — mesmo padrão já usado em `consultar_saldo`/`consultar_extrato`. 4 testes novos, build/lint/suite completa passando.
- **Achado real (2026-09-19): auto-apagar da mensagem com o `refresh_token` (`/registrar_email`) não sobrevivia a restart do bot — corrigido.** O aviso "se apaga sozinha em 5 minutos" (PR #254) dependia de um `setTimeout` só na memória do processo do bot; como o processo foi reiniciado (deploys dos dois achados acima) antes do timer disparar, a mensagem com o token ficou parada no chat por horas — mesma classe de bug do achado de confirmação cross-processo. **Corrigido** com tabela nova `mensagens_pendentes_apagar` (migration 0013): o agendamento passa a ser gravado no banco também (`agendarApagarPersistido`), e o bot varre por atrasados na subida (`index.ts`, antes de `bot.start`) apagando o que sobrou — o `setTimeout` continua como via normal (mais rápida, sem esperar reiniciar), a tabela é só o fallback de recuperação. 5 testes novos, build/lint/suite completa passando.
- **Checkpoint da Fase 7 fechado (2026-09-19) — teste manual completo em Homologação, confirmado pelo usuário.** Com os três achados acima corrigidos e implantados, o usuário repetiu o teste de ponta a ponta: e-mail de teste (PDF de fatura, "Cartão Visa Infinity") → `lerEmailFaturas --agora` → confirmação via Telegram → fatura gravada; depois `sincronizarCalendario --agora` → evento de vencimento criado no Google Calendar da conta vinculada. **Achado de teste (não é bug):** o evento no Calendar caiu no dia cadastrado como `dia_vencimento` do cartão (10/09/2026), não na data impressa no PDF de teste (25/09/2026) — comportamento correto por design: `calcularDataVencimentoFatura` (`src/relatorios/fluxoCaixa.ts:27-32`) sempre calcula o vencimento a partir do cadastro do cartão, nunca confia num valor extraído de um documento externo pra essa data; o PDF só serve pra identificar qual fatura/cartão e o valor. Rodar o job de novo não duplicou nem alterou o evento (idempotência via `evento_calendario_id` confirmada com dado real). Milestone "Fase 7 (e-mail e calendário)" fechado no GitHub (8/8 issues fechadas).
- **Fase 8 — em andamento (planejada 2026-09-19).** Agregação bancária via Open Finance (Pluggy). `tasks/plan.md`/`tasks/todo.md` reescritos (9 tarefas, 97-105), milestone "Fase 8 (Open Finance / Pluggy)" e issues #261-269 criadas. **Achado real de pesquisa, antes de escrever as tarefas** (docs.pluggy.ai): o Pluggy Connect Widget não tem URL hospedada como o OAuth do Google — exige SDK+backend próprio. Decisão de arquitetura: página HTML estática local (nunca publicada) + script que gera o `connect_token`, depois `/registrar_open_finance <item_id>` no bot pra vincular — mesmo princípio "sem servidor público" já adotado na Fase 7. Também decidido usar polling em vez de webhook, mesmo critério de segurança. Cobertura de conta PJ pelo "Meu Pluggy" segue não confirmada (ação que só o usuário pode fazer contatando o suporte da Pluggy) — não bloqueia o início da fase. **Tarefa 97 concluída (2026-09-20):** `env.ts` ganha grupo opcional `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` — mesmo padrão exato do grupo Google (Fase 7, Tarefa 90): ausentes = `env.pluggy === null` (integração desligada), incompleto = erro explícito, completo = `env.pluggy` populado. PR #273 mergeado (`Closes #261`). **Tarefa 98 concluída (2026-09-20):** migration `0014_open_finance.sql` — `contas_open_finance` (mapeamento `pluggy_account_id` único → conta/cartão do AutoFinance, exige um dos dois) e `transacoes_open_finance_processadas` (idempotência do job de sincronização, `pluggy_transaction_id` único), além de `transacoes` ganhar `origem`/`trace_id` (mesmo par que só `parcelas` tinha desde a Fase 1). **Tarefa 99 concluída (2026-09-20):** `src/integracoes/pluggy/cliente.ts` — client fino sobre `fetch` nativo (confirmado que não existe SDK oficial maduro pra Node antes de escrever REST manual): `autenticar`/`gerarConnectToken`/`obterItem`/`atualizarItem`/`listarContasDoItem`/`listarTransacoes` (pagina automaticamente). **Achado real:** o campo do connect token na resposta da Pluggy é `accessToken`, não `connectToken` como o nome da função sugere (confirmado via `docs.pluggy.ai/reference/authentication`). PR #275 mergeado (`Closes #263`). **Tarefa 100 concluída (2026-09-20):** `src/scripts/gerarConnectTokenPluggy.ts` (script manual, mesmo padrão de `configurarGoogleOAuth.ts`) + `scripts/pluggyConnectWidget.html` (página estática local, nunca publicada — login bancário acontece inteiramente no navegador do usuário, direto contra a Pluggy). **Achado real:** não existe URL de CDN oficialmente documentada pela Pluggy pra uso via `<script>` puro — usado o pacote `pluggy-connect-sdk` (npm) servido via jsDelivr. PR #276 mergeado (`Closes #264`). **Item pendente, não automatizável:** abrir a página de verdade e testar a conexão com uma conta sandbox da Pluggy (exige navegador real e conta em dashboard.pluggy.ai — mesmo caso do consentimento OAuth do Google, Fase 7). **Tarefa 101 concluída (2026-09-20):** comando `/registrar_open_finance <item_id>` (mesmo padrão do `/registrar_email`) — lista as contas do item, usuário responde "número = nome" mapeando pra conta/cartão já cadastrado (`resolverContaId`/`resolverCartaoId`), grava em `contas_open_finance` (upsert). Pendência (contas aguardando mapeamento) fica em Map em memória (`openFinancePendencia.ts`, mesmo padrão de `googleOAuthPendencia.ts`) — todo o fluxo acontece no mesmo processo do bot, sem o problema cross-processo da Fase 7. PR #277 mergeado (`Closes #265`), 839 testes passando (suite completa limpa). **Tarefa 102 concluída (2026-09-20):** `src/db/repositories/correspondenciaOpenFinance.ts` — `encontrarTransacaoManualCorrespondente`/`encontrarPagamentoFaturaOuParcelaCorrespondente` (mesma tolerância ±1%/±5 dias da Fase 7) e `pareceSaque`. **Achado real de pesquisa:** o campo `category` da Pluggy exige plano Pro e não documenta valores fixos — não dá pra confiar nele sozinho; `operationType` (`"SAQUE"`) só existe em conectores Open Finance mas é o sinal mais confiável, usado como sinal primário com fallback por texto na descrição. PR #278 mergeado (`Closes #266`). **Tarefa 103 concluída (2026-09-20):** `src/scripts/sincronizarOpenFinance.ts` — job periódico (mesmo intervalo/desenho de `lerEmailFaturas.ts`) que autentica na Pluggy, percorre `contas_open_finance` e aplica as duas checagens da Tarefa 102 antes de criar transação nova (`origem='open_finance'`); idempotência via `transacoes_open_finance_processadas` (mesmo padrão de `emails_processados`). **Três achados reais:** (1) conta Pluggy mapeada pra cartão (compra de crédito) exigiu estender `encontrarTransacaoManualCorrespondente` pra aceitar `{contaId} | {cartaoId}`, não só `contaId` como a Tarefa 102 cobria; (2) correspondência `'ambigua'` é tratada como encontrada (não cria transação, mesmo princípio de não decidir sozinho da Fase 7); (3) falha ao sincronizar uma conta (token expirado, item desconectado) não pode impedir sincronizar as demais — try/catch por conta dentro do loop. PR #279 mergeado (`Closes #267`), suite completa passando. **Tarefa 104 concluída (2026-09-20):** `src/scripts/renovarSandboxPluggy.ts` — job só ativo em Homologação (guard explícito por `env.ambiente === 'producao'`, não só `env.pluggy === null`, pra nunca rodar em Produção mesmo se `env.pluggy` fosse configurado por engano), chama `PATCH /items/{id}` uma vez por `pluggy_item_id` distinto a cada 20 dias, resetando a expiração do sandbox. Nenhuma peça de infra nova precisou ser criada — `atualizarItem`/`autenticar` (Tarefa 99) e `listarContasOpenFinance` (Tarefa 103) já existiam. PR #280 mergeado (`Closes #268`).
- **Menu "/" do Telegram com autocomplete (2026-09-19), a pedido do usuário.** Os 5 comandos do bot viviam só como regex soltas em `router.ts`, sem alimentar o menu nativo do Telegram (`bot.api.setMyCommands`). Nova fonte única `src/bot/comandos.ts` (`{comando, descricao, regex}[]`) usada tanto pelo roteamento quanto pelo `setMyCommands` (chamado uma vez na subida do bot, `index.ts`) — elimina o risco de as duas listas dessincronizarem. PR #270 mergeado em `development`, 802 testes passando (1 flake isolado de timeout já documentado).
- **Achado real (2026-09-19): dois deploys automáticos concorrentes colidem na VM.** Dois pushes em `development` em sequência rápida (merge do PR #270 + commit de docs logo em seguida) dispararam dois workflows de deploy quase ao mesmo tempo — o segundo falhou com conflito de nome de container (`docker compose up` tentando recriar o mesmo container que o primeiro deploy ainda não tinha terminado de recriar). Sem impacto real dessa vez (o segundo commit era só documentação, o primeiro deploy já tinha aplicado o código certo), mas é uma lacuna genuína do pipeline (sem lock/fila entre deploys) — mitigado por enquanto esperando cada deploy terminar (`gh run watch`) antes do próximo push; corrigir de vez (lock no workflow) fica como item futuro, não urgente pra um projeto solo.
- **Comando `/ajuda` (2026-09-19), a pedido do usuário.** Boa parte do que o bot sabe fazer é tool de IA chamada por linguagem natural (34 tools ao todo), não comando de barra — difícil de lembrar tudo. `/ajuda` lista os comandos de barra (mesma fonte `comandos.ts`) e um resumo por categoria do que dá pra só pedir conversando, em linguagem de usuário, sem citar nome interno de tool. PR #271 mergeado em `development`, 805 testes passando (suite completa limpa, sem flake dessa vez).
- **Relatórios um dia depois do período fechar + preço legível (2026-09-20), a pedido do usuário.** Relatório semanal passa a disparar segunda-feira 23h (não mais domingo) e o mensal no dia 1 do mês novo 23h (não mais no último dia do mês anterior) — os dois usam "ontem" como referência de janela, pra reportar o período que já fechou de vez, nunca o novo que acabou de começar. `verificarDespesasFixas.ts` mantém o disparo no último dia do mês (faz sentido avisar enquanto o mês ainda corre) — ganhou cópia própria da função de agendamento, já que divergiu do relatório mensal. Preço do alerta de preço (`monitorarPrecos.ts`) saiu de notação científica (`7.5e-7`) pra USD por 1M tokens (`US$ 0.7500/1M tokens`), mesma unidade do site do OpenRouter. **Achado real:** `"openrouter/auto"` (preço `-1`/`-1`, sentinela do OpenRouter pra modelo de preço variável) sempre aparecia como "mais barato" pra qualquer fluxo — corrigido excluindo preço negativo da comparação. PR #272 mergeado em `development`, 808 testes passando (suite completa limpa).
- **Próximo passo:** implementar a Tarefa 105 da Fase 8 (wiring `docker-compose.yml` + `npm audit` — fecha o código da fase).

## Erros conhecidos

- **`openai/gpt-5-nano` (modelo roteado pra `conversa_texto`) falha sistematicamente em chamar tools** — confirmado em 2026-09-08 via teste manual real em Homologação: 4 de 6 mensagens reais na mesma conversa (`Extrato mensal`, `Resumo Mensal`, `Extrato Mensal Conta Testes`, `Extrato Mensal Conta Testes Mês de Julho`) resultaram em `tool_calls: null` — o modelo respondia em texto livre pedindo `conta_id`/confirmação extra, ignorando a instrução explícita da descrição da tool ("não peça confirmação extra, chame diretamente"). **Ação:** `roteamento_tarefas.conversa_texto` trocado manualmente pra `openai/gpt-4o-mini` em Homologação (já tinha benchmark real de 92-100% de acurácia de tool calling, Tarefa 40). Ainda não rodado `rodar_benchmark_interno` formal contra `gpt-5-nano` pra confirmar objetivamente o tamanho do problema (achado só via uso real) — se quiser retomar esse modelo (mais barato) no futuro, rodar o benchmark antes.
- **PDF de comprovante não é lido — Gemini 2.5 Flash Lite via OpenRouter rejeita o formato na chamada multimodal** (confirmado em 2026-09-13 via teste manual real em Homologação, Fase 6 parte 12, Tarefa 82). O handler degrada como projetado ("ainda não consigo ler PDF, manda como foto"), sem travar — mas ler PDF de verdade continua fora de alcance com a arquitetura atual (data URI `application/pdf` em `image_url`). Suporte real exigiria endpoint dedicado de parsing de documento (ex: conversão prévia pra imagem, ou um modelo/API com suporte nativo a PDF) — não avaliado ainda, seria uma tarefa própria se isso virar prioridade.
- **`.env.producao` na VM está só como template (vazio)** — `TELEGRAM_BOT_TOKEN` e `TELEGRAM_ALLOWED_CHAT_IDS` sem valor, `AMBIENTE=homologacao` (errado). Confirmado em 2026-09-08 ao revisar a allowlist do Telegram: o serviço `producao` do `docker-compose.yml` nunca foi de fato subido na VM (só os serviços `*-homologacao` aparecem no `docker compose ps`), então isso não é um risco ativo hoje — mas precisa ser preenchido (token de bot novo e exclusivo pra Produção, chat_id real, `AMBIENTE=producao`) antes de rodar `docker compose up -d producao ...`, senão a validação Zod em `env.ts` recusa subir (falha segura, não é um bug de segurança, só um pré-requisito pendente).

## Histórico (índice)

Detalhe completo por sessão de trabalho vive em `docs/progresso/`, dividido por período pra não exigir carregar tudo de uma vez:

- [Planejamento (2026-08-27 a 2026-08-30)](docs/progresso/01-planejamento.md) — levantamento de requisitos completo, antes de qualquer código escrito.
- [Implementação (Fase 1 em diante, a partir de 2026-08-30)](docs/progresso/02-implementacao.md) — todo o histórico de código, testes reais em Homologação e achados, da Fase 1 até a rodada mais recente.
